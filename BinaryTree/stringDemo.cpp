#include<bits/stdc++.h>
#define MAX 50
using namespace std;
int main()
{
    char str[MAX];
    char str2[MAX];
   // memset(str2,p,sizeof(str2));
    //cin>>str;
    //fgets(str, MAX, stdin);
    cin>>str;
    cout<<str<<endl;
    cout<<strlen(str)<<endl;
    cout<<str2[5];
    return 0;
}
